<!DOCTYPE html>
<html>
  <head>
    <title>Title of the document</title>
    <style>
      .modalDialog {
        position: fixed;
        font-family: Arial, Helvetica, sans-serif;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        background: rgba(0, 0, 0, 0.1);
        z-index: 99999;
        -webkit-transition: opacity 400ms ease-in;
        -moz-transition: opacity 400ms ease-in;
        transition: opacity 400ms ease-in;
        color:white;

        pointer-events: none;
      }
      .modalDialog:target {
        opacity: 1;
        pointer-events: auto;
      }
      .modalDialog > div {
        width: 400px;
        position: relative;
        margin: 10% auto;
        padding: 5px 20px 13px 20px;
        border-radius: 10px;
        background: -moz-linear-gradient(#198754,#198754);
        background: -webkit-linear-gradient(#198754,#198754);
        background: -o-linear-gradient(#198754,#198754);
      }
      .close {
        background: #606061;
        color: #FFFFFF;
        line-height: 25px;
        position: absolute;
        right: -12px;
        text-align: center;
        top: -10px;
        width: 24px;
        text-decoration: none;
        font-weight: bold;
        -webkit-border-radius: 12px;
        -moz-border-radius: 12px;
        border-radius: 12px;
        -moz-box-shadow: 1px 1px 3px #000;
        -webkit-box-shadow: 1px 1px 3px #000;
        box-shadow: 1px 1px 3px #000;
      }
      .close:hover {
        background: #6ed1d8;
      }
      .button{
        -webkit-appearance: button;
-moz-appearance: button;
appearance: button;

        text-decoration:none;
        color:white;
        border-style:solid;
        border-width:10px;
        border-color:white;
        border-radius:.25rem;
        text-align:center;


      }
    </style>
  </head>
  <body>
    <!--<h2>Create Modal Box</h2>
    <a href="#openModal">Box</a>-->
    <div id="openModal" class="modalDialog">
      <div>
        <a href="#close" title="Close" class="close">X</a>
        <h2>Information </h2>
        <h2>Hey <%= username %>,</h2>
        <p>You have successfully registered</p>
        <p>While login use USERID as <%= userid%></p>
      </div>
    </div>

          <a style="text-decoration:none;color:green;margin-left:50%;margin-top:50%;" href="/login">Login</a>
  </body>
</html>
